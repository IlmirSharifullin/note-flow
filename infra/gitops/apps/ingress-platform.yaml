apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ingress-platform
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/IlmirSharifullin/note-flow
    targetRevision: HEAD
    path: infra/gitops/platform/ingress
  destination:
    server: https://kubernetes.default.svc
    namespace: edge
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
