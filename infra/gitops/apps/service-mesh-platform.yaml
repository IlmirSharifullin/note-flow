apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: service-mesh-platform
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/IlmirSharifullin/note-flow
    targetRevision: HEAD
    path: infra/gitops/platform/service-mesh
  destination:
    server: https://kubernetes.default.svc
    namespace: istio-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
